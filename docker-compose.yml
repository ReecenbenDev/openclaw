services:
  openclaw-gateway:
    image: ${REECENBOT_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      REECENBOT_GATEWAY_TOKEN: ${REECENBOT_GATEWAY_TOKEN}
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${REECENBOT_CONFIG_DIR}:/home/node/.openclaw
      - ${REECENBOT_WORKSPACE_DIR}:/home/node/.openclaw/workspace
    ports:
      - "${REECENBOT_GATEWAY_PORT:-18789}:18789"
      - "${REECENBOT_BRIDGE_PORT:-18790}:18790"
    init: true
    restart: unless-stopped
    command:
      [
        "node",
        "dist/index.js",
        "gateway",
        "--bind",
        "${REECENBOT_GATEWAY_BIND:-lan}",
        "--port",
        "${REECENBOT_GATEWAY_PORT:-18789}",
      ]

  openclaw-cli:
    image: ${REECENBOT_IMAGE:-openclaw:local}
    environment:
      HOME: /home/node
      TERM: xterm-256color
      BROWSER: echo
      CLAUDE_AI_SESSION_KEY: ${CLAUDE_AI_SESSION_KEY}
      CLAUDE_WEB_SESSION_KEY: ${CLAUDE_WEB_SESSION_KEY}
      CLAUDE_WEB_COOKIE: ${CLAUDE_WEB_COOKIE}
    volumes:
      - ${REECENBOT_CONFIG_DIR}:/home/node/.openclaw
      - ${REECENBOT_WORKSPACE_DIR}:/home/node/.openclaw/workspace
    stdin_open: true
    tty: true
    init: true
    entrypoint: ["node", "dist/index.js"]
